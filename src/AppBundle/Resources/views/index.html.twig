{% extends '::base.html.twig' %}

{% block body %}
    <div class="row">
        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
        <div class="col-md-2">
            <ul class="nav nav-pills nav-stacked topMargin">
                <li>
                    <a class="btn btn-success buttonTopMargin" href="{{ path('fos_user_profile_show') }}">
                        New portfolio
                    </a>
                </li>
                <li>
                    <a class="btn btn-success buttonTopMargin" href="{{ path('fos_user_security_logout') }}">
                        Active portfolios
                    </a>
                </li>
            </ul>
        </div>
        {% else %}
            <div class="row">
                <div class="col-md-8">
                    <h1 class="text-center">Welcome to MarketMadness!</h1>
                </div>
            </div>
        {% endif %}

        <div class="row">
            {% include('AppBundle:tables:stockPrices.html.twig') %}
        </div>

    </div>

{% endblock %}