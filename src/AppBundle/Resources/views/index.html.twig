{% extends '::base.html.twig' %}

{% block body %}
    <div class="row">
        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
            {% include('@App/_sideBar.html.twig') %}
        {% else %}
            <div class="row">
                <div class="col-md-8">
                    <h1 class="text-center">Welcome to MarketMadness!</h1>
                </div>
            </div>
        {% endif %}

        <div class="col-md-8">
            {% include('@App/_stockTable.html.twig') %}
            {% if sharedTransactions %}
                <h3>Shared transactions</h3>
                <table class="table">
                    <tr>
                        <th>Date</th>
                        <th>Owner</th>
                        <th>Type</th>
                        <th>Stock</th>
                        <th>Price</th>
                        <th>Comment</th>
                    </tr>
                    {% for transaction in sharedTransactions %}
                        <tr>
                            <td>{{ transaction.date|date('d/m H:i') }}</td>
                            <td>{{ users[transaction.id] }}</td>
                            <td>{{ transaction.transactionType }}</td>
                            <td>{{ transaction.stockSymbol }}</td>
                            <td>{{ transaction.stockPrice|number_format(2) }}$</td>
                            <td><a class="btn btn-default commentTransactionButton" href="#">
                                    Comment
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                </table>
            {% else %}
                <h3>Nobody shared a transaction yet! Be first!</h3>
            {% endif %}
        </div>
    </div>

{% endblock %}